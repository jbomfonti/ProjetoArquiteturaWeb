
services:
  postgres:
    image: 'bitnami/postgresql:latest'
    container_name: arquiteturaweb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pgarquiteturaweb
    ports:
      - "5433:5432"
    volumes:
      - database:/var/lib/postgresql/data

volumes:
  database:
    